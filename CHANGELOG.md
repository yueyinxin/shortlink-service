# 变更日志

本文件记录本项目的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## [未发布]

### 计划中

- 分布式锁，支持统计刷库任务的多实例部署（见架构文档「已知风险」）
- 链接批量导入导出
- 访问密码保护

---

## [1.0.0] - 2026-09-24

首个正式版本，覆盖需求文档中定义的全部 v1.0.0 范围。

### 新增

**认证**

- 用户注册：用户名、邮箱唯一性由数据库唯一索引保证；密码以 BCrypt(cost=10) 存储
- 用户登录：支持用户名或邮箱登录
- 令牌刷新：双令牌方案，refresh token 一次性使用（Redis `GETDEL` 原子消费）
- 用户登出：作废 refresh token
- 当前用户信息查询

**短链**

- 创建短链：自动生成 6 位短码，或使用自定义短码（4-10 位字母数字）
- 短码生成：Feistel 网络格式保留加密 + Cycle-Walking，无冲突且不可枚举
- 短链跳转：Redis 缓存 + 302 重定向，缓存故障时降级直查数据库
- 短链管理：分页查询、关键字搜索、启用/禁用、删除
- 过期时间：可设置或留空表示永不过期

**统计**

- 按天聚合的 PV / UV 统计
- PV 使用 Redis 计数器（增量语义），UV 使用 HyperLogLog（绝对值语义）
- 定时刷库：每 60 秒将 Redis 计数落库，使用 UPSERT 保证幂等
- 读时合并：查询时合并未落库增量，对外表现始终最新
- 稀疏日期补零，便于前端直接绘图

**安全**

- 统一错误码与响应信封，4xx 不打堆栈、5xx 不泄露异常消息
- 水平越权防护：归属校验作为 SQL 查询条件，跨用户访问返回 404
- 开放重定向防护：URL 协议白名单（仅 http/https）+ 跳转前二次校验
- 用户名枚举防护：统一错误信息 + 占位哈希抹平响应时间差
- 限流：跳转按 IP（300 次/分）、创建按用户（30 次/分），Redis Lua 原子计数
- 分页上限 100，防止内存耗尽型拒绝服务
- Actuator 仅放行 health，其余端点需认证
- 刷新令牌以 SHA-256 哈希形式存储，Redis 数据泄露不等于令牌泄露

**可观测性**

- 结构化的 traceId 贯穿日志，响应头回传，可与用户报障关联
- `/actuator/health` 区分存活与就绪探针
- `/actuator/prometheus` 输出 HTTP 耗时直方图与 JVM 指标

**工程**

- 分层架构：`web → application → domain ← infrastructure`
- 按功能垂直切分包，功能域内部再分层
- ArchUnit 架构测试守护 5 条分层约束
- 77 个单元测试，含对 4 位短码空间全部 14,776,336 个取值的穷举验证
- Testcontainers 集成测试（真实 MySQL / Redis，不使用 H2）
- GitHub Actions CI：编译、单元测试、集成测试、镜像构建与冒烟测试
- Flyway 迁移脚本，DDL 变更有版本化审计轨迹
- 多阶段 Dockerfile，非 root 运行，容器感知的 JVM 参数
- 生产部署编排与一键部署脚本
- 启动自检：检测到开发默认密钥时拒绝启动

### 变更

- **短码生成算法**：由线性同余打散改为 Feistel 格式保留加密。
  原方案虽然数学上双射，但相邻输出的差值恒为常数，
  攻击者可通过解方程还原映射并枚举全站短码。
  详见 [ADR-0002](docs/adr/0002-short-code-generation.md)。
  该缺陷已固化为回归测试。
- **`RateLimitFilter` 的位置**：从 `common.ratelimit` 移至 `security`。
  原位置造成 `common → auth` 的反向依赖，由架构测试捕获。

### 已知限制

见 [README「已知限制」](README.md#已知限制)与[架构设计 §8](docs/02-architecture.md#8-已知架构风险)。
主要项：MySQL / Redis 为单点；统计刷库任务不支持多实例；搜索无法使用索引；
**尚未在真实环境上线运行**。

---

## 版本号规则

| 变更类型 | 版本位 | 示例 |
| --- | --- | --- |
| 破坏性变更（接口不兼容、数据模型不兼容） | major | 1.0.0 → 2.0.0 |
| 新增功能（向后兼容） | minor | 1.0.0 → 1.1.0 |
| 缺陷修复或性能优化（向后兼容） | patch | 1.0.0 → 1.0.1 |

**关于数据库迁移与版本号**：新增 Flyway 迁移脚本（即使是纯新增列）通常对应 minor 版本；
修改或删除已有列属于破坏性变更，对应 major 版本。
已发布的迁移脚本绝不可修改 —— Flyway 会校验校验和，修改会导致启动失败。
