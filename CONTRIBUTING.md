# 贡献指南

本文档说明本项目的开发规范。这些规范不是为了形式，每一条都对应着一类具体的失败模式。

---

## 1. 开发环境

### 1.1 前置要求

| 工具 | 版本 | 说明 |
| --- | --- | --- |
| JDK | 17 | `java -version` 应显示 17.x |
| Docker | 20+ | 用于本地基础设施与集成测试 |
| Docker Compose | v2 | `docker compose version` |

Maven **不需要**单独安装 —— 项目自带 Maven Wrapper（`./mvnw`），
它会自动下载并使用 `pom.xml` 中约定版本的 Maven。
这样做的目的是保证所有开发者与 CI 使用完全相同的构建工具版本，
避免"在我机器上能构建"这类问题。

### 1.2 启动

```bash
# 1. 启动基础设施（MySQL + Redis）
docker compose up -d
docker compose ps          # 等两个服务都显示 healthy

# 2. 运行应用
./mvnw spring-boot:run
```

应用启动时会自动执行 Flyway 迁移建表。首次启动后可以访问：

- 接口文档：http://localhost:8080/swagger-ui.html
- 健康检查：http://localhost:8080/actuator/health

### 1.3 常见问题

**Docker 拉取镜像失败**：如果本机配置了 HTTP 代理，Docker daemon 会强制使用它。
代理未运行时，所有拉取都会失败且报错信息指向 `127.0.0.1:7897` 之类的地址。
解决办法是启动代理，或为镜像仓库配置国内加速源。

**应用启动报 `Schema-validation: missing table`**：说明 Flyway 迁移未执行或未执行完。
检查 `docker compose ps` 中 MySQL 是否 `healthy`，以及日志中是否有 Flyway 的错误。

**应用启动报 `JWT 密钥至少需要 32 字节`**：JWT 密钥长度不足。
开发环境下 `application.yml` 已提供满足长度的默认值，出现这个错误通常是
自定义了 `SHORTLINK_JWT_SECRET` 环境变量但值太短。

---

## 2. 分支模型

采用简化版 Git Flow：

```
main ────●────────────●────────────●──────▶  仅接受通过 PR 的合并，始终可发布
          \          /              \
           ●───●────●                ●───●   feature/*  功能开发
                \                        \
                 ●───●                    ●  fix/*      缺陷修复
```

| 分支 | 命名 | 来源 | 合并到 |
| --- | --- | --- | --- |
| `main` | — | — | — |
| 功能 | `feature/简短描述` | `main` | `main` |
| 修复 | `fix/简短描述` | `main` | `main` |
| 文档 | `docs/简短描述` | `main` | `main` |
| 重构 | `refactor/简短描述` | `main` | `main` |
| 紧急修复 | `hotfix/简短描述` | `main` 的 tag | `main` 并打新 tag |

**`main` 分支必须始终处于可发布状态。** 这意味着每次合并前 CI 必须全绿，
且不允许直接推送到 `main`。

---

## 3. 提交信息规范

采用 [Conventional Commits](https://www.conventionalcommits.org/)。

```
<类型>(<范围>): <简短描述>

<详细说明：为什么这样改，而不是改了什么>

<关联的 issue>
```

### 3.1 类型

| 类型 | 用途 | 会触发版本号变更 |
| --- | --- | --- |
| `feat` | 新功能 | minor |
| `fix` | 缺陷修复 | patch |
| `docs` | 仅文档 | 否 |
| `style` | 格式调整（不影响逻辑） | 否 |
| `refactor` | 重构（不新增功能、不修缺陷） | 否 |
| `perf` | 性能优化 | patch |
| `test` | 测试相关 | 否 |
| `build` | 构建系统或依赖变更 | 否 |
| `ci` | CI 配置变更 | 否 |
| `chore` | 其他杂项 | 否 |
| `revert` | 回滚 | — |

**破坏性变更**在类型后加 `!`，或在正文中写 `BREAKING CHANGE:`，触发 major 版本。

### 3.2 示例

```
feat(link): 支持为短链设置过期时间

过期时间存储为可空字段，null 表示永不过期。判定逻辑放在 LinkSnapshot 上，
使缓存路径与数据库路径共用同一份实现，避免两处判定不一致。

关联 #42
```

```
fix(auth): 登录时抹平账号不存在与密码错误的响应时间差

原先用户不存在时直接返回，跳过了 BCrypt 校验，响应明显更快。
攻击者据此可以枚举出系统内已注册的用户名。

修复方式是在用户不存在时也执行一次针对占位哈希的比对。占位哈希对应一个
随机生成的、无人知晓的密码，因此永远不可能匹配成功。

关联 #57
```

### 3.3 为什么文档里要写"为什么"

diff 本身已经说明了"改了什么"。**唯一无法从代码中恢复的信息是"为什么"** ——
为什么选这个方案而不是另一个、当时的约束是什么、否决了哪些替代方案。
缺少这些，后来的维护者只能靠猜测，而猜错的代价通常是重复踩同一个坑。

如果改动涉及架构层面的决策，还应当在 `docs/adr/` 中新增一篇 ADR。

---

## 4. 代码规范

### 4.1 分层依赖

```
web ──▶ application ──▶ domain ◀── infrastructure
```

**依赖只能自上而下，不能反向或跨层跳跃。**

- `domain` 包中不允许出现 `org.springframework`、`jakarta.persistence`、`redis` 的 import
- 控制器中不允许直接访问 Repository
- 业务逻辑不允许写在控制器或 Repository 中

这条规则的意义是让核心逻辑可以脱离框架做单元测试。它容易被违反，
因此在代码评审中需要专门检查。

### 4.2 注释

**只写代码本身无法表达的信息。**

需要有注释的：

- 某个"反直觉"的选择背后的原因（例如"这里不用 `save` 是因为 INSERT 会被推迟到事务提交"）
- 某个看似多余的检查防的是什么问题（例如"这个长度上限防的是 BCrypt 静默截断"）
- 参数取某个具体值的依据（例如"TTL 取 60 秒是因为……"）

不需要注释的：

- 代码在做什么（`// 遍历列表` —— 读代码就知道）
- 本次改动的内容（`// 新增了过期校验` —— 那种信息属于提交信息）
- 修正记录（`// 修复了之前的 bug` —— 那属于提交历史）

### 4.3 安全性

评审时必须确认以下几点：

| 检查项 | 说明 |
| --- | --- |
| 用户输入是否用于拼接 SQL | 必须使用参数化查询 |
| 用户输入是否回显到日志 | 令牌、密码、邮箱等需要脱敏 |
| 是否记录了敏感信息 | 密码明文、令牌明文绝不可进日志 |
| 越权 | 查询条件是否包含 `userId`；返回 404 而非 403 |
| 错误响应 | 5xx 是否泄露了堆栈或异常消息 |
| 新增的公开端点 | 是否有意为之，是否在 SecurityConfig 中显式声明 |

---

## 5. 测试要求

| 层次 | 命名 | 执行时机 | 要求 |
| --- | --- | --- | --- |
| 单元测试 | `*Test.java` | `mvn test` | 不起 Spring 上下文，不依赖外部资源 |
| 集成测试 | `*IT.java` | `mvn verify` | 可用 Testcontainers 启动真实依赖 |

**新增功能必须附带测试。** 具体而言：

- 领域逻辑（`domain` 包）：必须覆盖全部分支与边界条件
- 新增接口：至少覆盖正常路径与主要的错误路径
- 缺陷修复：必须先写一个复现该缺陷的测试，确认它失败，再修复

最后一条尤为重要。没有复现测试的修复无法证明问题被解决了，
也无法防止它被再次引入。

### 5.1 不要用 H2 做集成测试

H2 与 MySQL 在 `ON DUPLICATE KEY UPDATE` 语法、`LIMIT` 分页、字符集与排序规则、
`DATETIME` 精度上都有差异。在 H2 上通过的测试不能保证 MySQL 上也通过 ——
**这类"假绿"测试比没有测试更危险**，因为它让人对结论产生错误的信心。

---

## 6. 提交前自查

在推送前执行：

```bash
./mvnw -B verify      # 编译 + 单元测试 + 集成测试 + 覆盖率报告
```

并确认：

- [ ] 提交信息符合 Conventional Commits 规范
- [ ] 新增/修改的逻辑有对应测试
- [ ] 新增的配置项已在 `application.yml` 中给出默认值，并在 `application-prod.yml` 中列出
- [ ] 没有把密钥、令牌、真实域名硬编码进代码或配置文件
- [ ] 如果修改了数据模型，Flyway 迁移脚本与实体类在**同一个提交**中
- [ ] 已发布的迁移脚本**未被修改**（Flyway 会校验校验和，修改会导致启动失败）
